server {
    listen 80;
    server_name taskboard.local;

    root /usr/share/nginx/html;
    index index.html;

    # Serve file statici per la SPA Angular
    location / {
        try_files $uri $uri/ /index.html;
    }
}
